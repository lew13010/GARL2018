{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gamer</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gamer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gamer.name }}</td>
            </tr>
            <tr>
                <th>Steam</th>
                <td>{{ gamer.steam }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('gamer_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('gamer_edit', { 'id': gamer.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
